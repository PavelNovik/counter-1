import {StateType} from "./store";

const state: StateType = {
    counter: 0,
    maxVal: 5,
    startVal: 0,
    isSettings: false,
    error: false,
    userMessage: null
}
test('Get state', ()=> {

})